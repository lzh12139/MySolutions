#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll a[1010],base[1010];
const ll mod=9999991;
ll ans[mod+10];
int main(){
    int t;scanf("%d",&t);
    while(t--){
        int n,m;scanf("%d%d",&n,&m);
        for(int i=0;i<=n;i++)scanf("%d",&a[i]);
        ans[0]=a[0];
        solve(a,)
    }
}